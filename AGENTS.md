# 版本与部署规范

1. 从 V1.0.0 开始管理发布版本。重大不兼容变更递增主版本号，
   新功能递增次版本号，问题修复递增修订号。

2. 每个发布版本唯一对应一个 Git 提交和一套构建产物。
   同一产物从测试环境发布到生产环境时沿用版本号；
   内容发生变化必须使用新版本号，不得覆盖已有版本。

3. 部署前，必须将本次发布涉及的代码和发布说明提交并推送到远程仓库，
   创建并推送同名 Git 标签，例如 V1.0.0。
   构建产物必须来自该标签对应的提交，不得包含未提交的修改。

4. 发布说明统一保存在 docs/RELEASE_NOTES.md，按版本倒序追加。
   每个版本记录发布日期、对比基准版本、新增功能、问题修复、
   配置或数据库变更及已知问题。首个版本记录初始功能范围。

5. 每次部署记录目标环境、部署时间、版本号、Git 提交编号、
   该环境部署前的版本及部署结果。回滚也需要记录。

6. 部署前必须通过本次变更所需的测试和构建检查。
   密钥、密码、患者数据及本地环境文件不得提交到仓库。

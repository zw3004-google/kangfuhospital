# 阶段5：开发环境企业微信联调

> 执行日期：2026-08-31～2026-09-01  
> 当前状态：**已完成并通过验收**

## 已完成

- 已生成本机专用的《开发环境企微连通测试数据》Markdown，并通过 `.gitignore` 排除，避免 Secret 进入版本库。
- 后端通过 `WECOM_CORP_ID`、`WECOM_AGENT_ID`、`WECOM_SECRET` 环境变量加载开发环境应用参数，健康检查为 `UP`。
- 已在本地开发数据库准备五名测试接收人，工号、姓名、企微 ID、科室和角色匹配一致：
  - 科主任：彭百成；
  - 主管医生：王冰芯；
  - 随访员：聂文斌；
  - 营养科人员：卢倩；
  - 居家康复人员：高颖。
- 已创建五条带“开发环境企微连通测试”标识的真实发送任务。
- DNS 解析正常；后端可以建立 HTTPS 连接并调用企业微信接口。
- 任务调度、领取、发送尝试落库及 1 分钟自动重试已验证。

## 当前联调结果

五条任务均收到企业微信错误码：

```text
60020 not allow to access from your ip
```

企业微信返回的当前出口 IP：

```text
117.128.105.65
```

这表明 Corp ID、Agent ID、Secret、DNS、TLS 和企业微信 API 地址已进入真实调用链路；消息尚未送达接收人，阻塞点为企业微信应用的可信 IP/出口 IP 白名单。

### 2026-09-01 复测

- 常规公网 IP 查询服务一致显示 `104.225.144.127`，该地址已配置为企微可信 IP。
- 系统批量人工重发接口成功受理 5 条任务，返回数量为 5。
- 五条任务均新增 `trigger_type=MANUAL` 的第 5 次发送尝试，证明批量人工重发与历史保留正常。
- 企业微信消息发送接口仍返回 `60020`，并明确报告 Java 后端请求来源为 `117.128.105.65`。

结论：公网 IP 查询请求与 Java 后端直连企微请求经过不同网络出口。企微可信 IP 判断必须以消息发送接口返回的来源 IP 为准，因此仍需将 `117.128.105.65` 加入可信 IP；`104.225.144.127` 可保留，以覆盖当前另一条出口路径。

### 2026-09-01 可信 IP 配置后复验

- 企业微信后台同时配置两条出口后，将五条 `RETRYING` 开发测试任务的计划时间提前至当前时间进行复验。
- 彭百成、王冰芯、聂文斌、卢倩、高颖五名接收人的任务全部转为 `SENT`。
- 成功发送时间：2026-09-01 08:49:26～08:49:27（Asia/Shanghai）。
- 五条最新尝试均为 `AUTOMATIC / SENT`，无企微错误码，主任务 `last_error` 全部清空。
- 尝试历史汇总：自动失败 25 次（`60020`）、人工重发失败 5 次（`60020`）、可信 IP 生效后自动成功 5 次。
- 批量人工重发审计记录共 5 条，`action_type=BATCH_MANUAL_RETRY`。
- 五条成功任务覆盖 5 个不同企微 ID，接收人匹配和真实发送技术链路通过。

阶段5目前不再受网络、可信 IP、应用可见范围或客户端确认阻塞。五名接收人均已确认收到；当前仅需完成开发环境 11:00 出院提醒和欠费通告的自动生成及发送验收。

### 单条人工重发复验

- 创建不含真实患者信息的单条失败测试任务，接收人为随访员聂文斌。
- 通过系统 `POST /api/arrears/push-records/{id}/retry` 接口执行单条人工重发。
- 主任务由 `FAILED` 转为 `SENT`，成功时间为 2026-09-01 08:55:28（Asia/Shanghai）。
- 尝试序列保留预置失败记录，随后新增 `MANUAL / SENT` 成功记录，错误码为空，`last_error` 清空。
- 审计日志新增 1 条 `MANUAL_RETRY`。

至此，真实企微连通、五类接收人发送、失败记录、1/5/15 分钟自动重试、可信 IP 生效后的自动恢复、单条人工重发和批量人工重发技术链路全部通过。

### 2026-09-01 消息格式与未到达排查

- 聂文斌反馈未收到消息。直接调用消息接口返回 `errcode=0`、`errmsg=ok`、有效 `msgid`，且未返回 `invaliduser`。
- 通讯录接口确认 `BD-NIEWENBIN006` 对应聂文斌、`enable=1`，但返回 `status=4`；应由企微管理员核对该成员是否已激活并加入当前企业，同时由本人检查应用入口及消息免打扰设置。
- 后端已增强成功判定：即使 `errcode=0`，只要返回 `invaliduser`、`invalidparty` 或 `invalidtag`，仍按失败记录并进入重试，不再误记成功。
- 已按更新后的开发测试数据向科主任彭百成发送完整欠费通报格式消息。
- 已发送多记录合并测试：科主任、主管医生、随访员、营养科、居家康复各一条，每条包含 4 名脱敏患者及实际格式住院号。
- 六条格式测试任务均首次发送成功，状态为 `SENT`，无错误码。
- 随访、营养、居家康复及非计划出院的定时生成代码已改为按接收角色合并患者，正文包含脱敏姓名和住院号。
- 新增企微响应判定单元测试 2 个，全部通过；后端已重启并健康检查 `UP`。

### 聂文斌新企微 ID 复测

- 测试数据将聂文斌企微 ID 由 `BD-NIEWENBIN006` 更新为 `niewenbin`，本地开发数据库已按工号 `03563` 同步更新。
- 通讯录查询新 ID 返回 `60011`，当前应用无权访问该成员或该成员不在应用通讯录可见范围。
- 系统向 `niewenbin` 发送包含两条脱敏随访记录的复测消息。
- 企业微信连续返回 `81013 user & party & tag all invalid`，任务进入 `RETRYING`，消息未送达。
- 结论：`niewenbin` 对当前 Corp ID / Agent ID 无效，或未加入应用可见范围。需企微管理员在当前企业后台复制成员账号字段并检查应用可见范围，确认后再次更新测试数据。

#### 加入应用可见范围后复验

- 企微管理员将 `niewenbin` 加入应用可见范围后，立即提前执行原任务的下一次自动重试。
- 任务 13 于 2026-09-01 09:50:26（Asia/Shanghai）转为 `SENT`。
- 最新第 4 次尝试为 `AUTOMATIC / SENT`，错误码为空，主任务 `last_error` 已清除。
- 结论：新企微 ID `niewenbin` 正确，先前未收到及 `81013` 的直接原因是成员未加入应用可见范围。

## 开发变更汇总

- `WeComClient` 由仅判断 `errcode` 调整为同时校验 `invaliduser`、`invalidparty` 和 `invalidtag`，无效目标不会再被误记为成功。
- `DischargeReminderScheduler` 已重构随访、营养、居家康复和非计划出院消息生成：按角色/接收人合并、患者姓名脱敏、携带实际住院号、多条记录换行展示。
- 测试接收人映射按工号维护；聂文斌企微 ID 从旧值更新为 `niewenbin`。
- 新增 `WeComClientTest` 两个响应判定测试，0 失败、0 错误；后端重启后健康检查为 `UP`。

## 最终测试结果摘要

| 验证项 | 结果 | 证据摘要 |
|---|---|---|
| Corp ID、Agent ID、Secret 环境变量加载 | 通过 | 后端健康且真实接口调用成功，源码与普通文档不保存 Secret |
| DNS、TLS、企微 API 连通 | 通过 | 真实消息接口返回并生成发送记录 |
| 可信 IP | 通过 | 两个网络出口均配置后，原 `60020` 任务自动恢复为 `SENT` |
| 五类接收人 | 通过 | 科主任、主管医生、随访员、营养科、居家康复均有成功发送记录 |
| 欠费通报格式 | 通过 | 完整欠费通报已发送给科主任彭百成 |
| 多记录合并与脱敏 | 通过 | 五类角色分别发送 4 条合并记录，包含脱敏姓名和住院号 |
| 自动重试 | 通过 | 首次失败后 1/5/15 分钟尝试历史完整 |
| 单条及批量人工重发 | 通过 | `MANUAL_RETRY`、`BATCH_MANUAL_RETRY` 审计记录完整 |
| 无效接收目标判定 | 通过 | 新 ID 未进入可见范围时返回 `81013`；加入后原任务重试成功 |
| 08:00 真实业务定时生成 | 待验收 | 需准备符合日期条件的真实业务测试记录 |
| 五名接收人客户端最终确认 | 通过 | 2026-09-01 用户确认五名接收人均已收到测试信息 |

### 定时任务临时调整与测试数据

- 定时表达式已由硬编码改为配置项 `app.messaging.discharge-cron`，环境变量为 `APP_MESSAGING_DISCHARGE_CRON`。
- 默认值仍为每天 08:00（`0 0 8 * * *`），保持生产业务基线不变。
- 本次开发环境为加快验收，运行时临时配置为每天 11:00（`0 0 11 * * *`）。
- 已准备5名专用开发测试患者：4名覆盖 7/30/60 天随访、今日营养会诊及今日居家康复会诊，1名覆盖昨日非计划出院。
- 11:00 预期生成：随访员1条合并消息（4名患者）、营养科1条合并消息（4名患者）、居家康复1条合并消息（4名患者）、主管医生1条非计划出院消息。
- 后端已按 11:00 配置重启，健康检查为 `UP`。

### 欠费通告定时任务

- 阶段 5 验收时新增 `ArrearsNotificationScheduler`，当时生产默认每周一 08:00 执行。该配置已在 2026-09-01 通报报表原型对齐阶段 4 中调整为每天 08:00；配置项仍为 `app.messaging.arrears-cron`，环境变量仍为 `APP_MESSAGING_ARREARS_CRON`。
- 本次开发环境临时配置为每天 11:00，与出院提醒同时进行定时触发验收。
- 通告从当前 `in_arrears=true AND payment_status=UNPAID` 数据动态汇总总额和科室排名，发送给所有启用的科主任、主管医生。
- 当前数据模型没有独立的“出院已结算欠费”字段，因此该项明确显示为 `0.00万`；在院按 `discharged_at IS NULL` 计算，出院未结算按已出院且未缴费计算，避免伪造无法验证的数据口径。
- 已准备6条专用欠费测试记录，覆盖6个科室，动态汇总合计为 `107.65万`。
- 新增金额元转万元两位小数单元测试并通过。
- 11:00 预期额外生成2条欠费通告任务，分别发送给测试科主任彭百成和主管医生王冰芯。

## 11:00 定时触发最终验收

2026-09-01 11:00（Asia/Shanghai）开发环境自动生成 6 条计划任务：

- 欠费通告 2 条：主管医生王冰芯、科主任彭百成；
- 营养会诊 1 条：卢倩，4 名患者合并；
- 居家康复会诊 1 条：高颖，4 名患者合并；
- 随访提醒 1 条：聂文斌，4 名患者合并；
- 非计划出院 1 条：王冰芯。

6 条任务均在 11:00:34～11:00:36 首次自动尝试发送成功，状态为 `SENT`，失败尝试为 0，`last_error` 为空。欠费通告包含预期汇总 `107.65万`，患者类消息均包含脱敏姓名；随访、营养和居家康复均为 4 行合并记录并携带住院号。

结论：11:00 定时生成、动态内容、合并格式和真实发送全部通过，阶段 5 正式关闭，项目全面进入阶段 6。

## 后续运维事项

1. 开发网络存在分流时，应长期保留两个可信出口，或由网络管理员提供稳定 NAT 出口。

## 阶段6启动判断

- 当前结论：阶段 5 **已通过并关闭**。
- 阶段 6 并行门禁已解除，项目当前阶段为阶段 6。

## 安全说明

- 本记录不保存 Secret、access token 或完整请求 URL。
- Secret 仅通过运行进程环境变量注入。
- 本地敏感测试数据文件不纳入版本控制。

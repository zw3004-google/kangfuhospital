# 阶段6：系统测试与整改

> 启动日期：2026-09-01  
> 当前状态：**阶段5已关闭；阶段6自动化范围、各页面原型对齐及预计出院管理患者工作台补充整改均已完成**

## 启动结论

阶段 5 的真实企微连通、五类接收人、消息格式、重试、客户端确认及开发环境 11:00 定时触发均已通过，阶段 5 已正式关闭。阶段 6 既有范围、各页面原型对齐及预计出院管理患者工作台补充整改均已完成。最新复验结果为：Flyway V1～V21、后端 85 项测试、前端 33 项测试、类型检查和生产构建全部通过，26 项容器集成测试实际执行且 0 跳过。人工角色 UAT 和生产环境验证不包含在本次自动化结论中。

## 新增整改计划

- [欠费明细原型对齐修改计划](欠费明细原型对齐修改计划.md)：补齐统计、筛选、明细字段、结构化追缴状态、编辑历史和导入反馈，并同时支持 Excel 与 CSV 导出；按确认结论不恢复原型页面底部业务说明。
- 该计划第一至第十阶段已完成，最终全量自动回归结果记录于本文件“欠费明细原型对齐第十阶段执行记录”。
- [通报报表原型对齐实施记录](通报报表原型对齐实施记录.md)：记录报表批次一致性、DataScope、患者 Top10、Top3/科室排行、只读通报内容、每天 08:00 企微任务、Excel 导出及阶段 1～5 测试结果。
- [欠费推送记录原型对齐修改计划](欠费推送记录原型对齐修改计划.md)：删除渠道筛选及渠道列，固化四种页面业务状态、推送日期口径、科主任/主管医生 DataScope、查看与重发权限拆分及按勾选 ID 批量重发方案；阶段 1～7 已完成并通过自动化验收。
- 预计出院管理原型对齐第一至第八阶段及患者工作台补充整改记录见本文件末尾；最终集成回归覆盖导航、字段权限、导入、导出、筛选、统计、编辑、历史、会诊和随访相关功能。

## 验收范围

1. 全量自动回归：后端单元测试、PostgreSQL/Testcontainers 集成测试、Flyway 空库迁移、前端类型检查和生产构建。
2. 批量与容量：欠费和预出院各验证 1、999、1000、1001 条边界；1000 条以内完成导入，1001 条明确拒绝且不产生半批数据；重复导入保留人工字段。
3. 性能：1000 条导入、全量列表查询、筛选、统计和导出记录耗时与资源占用；按项目基线验证不超过 100 并发登录用户、每日约 50 次重复导入和约 1000 条企微任务的容量。
4. 五类角色端到端 UAT：系统管理员、运营部、财务科、科主任、主管医生分别覆盖登录、菜单、查询、编辑、导入、导出、统计、越权和审计。
5. 缺陷整改：缺陷必须记录严重度、复现步骤、影响范围、修复版本、回归证据和关闭结论；P0/P1 清零后方可结束阶段 6。

## 通过标准

- 自动测试 0 失败、0 错误、0 跳过，Testcontainers 测试实际执行。
- Flyway V1～V21 能在 PostgreSQL 16 空库完整迁移。
- 前端类型检查和生产构建成功，主入口 JavaScript 不超过 100 KB。
- 1000 条导入边界、事务回滚、重复覆盖、人工字段保留及导出数据范围全部通过。
- 五类角色 UAT 通过，跨科室/跨医生越权请求返回 403。
- 无未关闭 P0/P1 缺陷；P2/P3 均有明确处理结论。
- 阶段 5 的 11:00 定时验收通过并正式关闭。

## 首轮执行记录

执行时间：2026-09-01 10:28（Asia/Shanghai）

| 检查项 | 结果 | 证据 |
|---|---|---|
| Docker/Testcontainers 环境 | 通过 | Docker Desktop 4.63.0、Engine 29.2.1、`desktop-linux` |
| Flyway 空库迁移 | 通过 | PostgreSQL 16，V1～V13 共 13 个迁移成功 |
| 后端自动测试 | 通过 | 31 个测试，0 失败、0 错误、0 跳过；其中 PostgreSQL 业务集成测试 17 个 |
| 前端类型检查与构建 | 通过 | `vue-tsc -b && vite build` 成功 |
| 前端入口包限制 | 通过 | 入口 JavaScript 12.06 KB，小于 100 KB |
| 阶段 5 定时门禁 | 通过 | 11:00 自动生成 6 条任务，首次发送全部成功 |

## 执行清单

| 编号 | 测试项 | 状态 |
|---|---|---|
| S6-01 | 阶段 0～4 全量自动回归 | 已通过 |
| S6-02 | 欠费导入 1/999/1000/1001 条边界 | 已通过 |
| S6-03 | 预出院导入 1/999/1000/1001 条边界 | 已通过 |
| S6-04 | 重复导入、事务回滚和人工字段保留 | 已通过：重复覆盖保留人工字段，重复键及超限批次无业务写入 |
| S6-05 | 列表、筛选、统计与导出容量/耗时 | 已通过 |
| S6-06 | 100 并发登录与核心只读接口 | 已通过 |
| S6-07 | 系统管理员人工 UAT | 按项目方要求本轮不执行；自动化权限回归已通过 |
| S6-08 | 运营部人工 UAT | 按项目方要求本轮不执行；自动化权限回归已通过 |
| S6-09 | 财务科人工 UAT | 按项目方要求本轮不执行；自动化权限回归已通过 |
| S6-10 | 科主任人工 UAT 与跨科室 403 | 人工 UAT 本轮不执行；自动化跨科室 403 已通过 |
| S6-11 | 主管医生人工 UAT 与跨医生 403 | 人工 UAT 本轮不执行；自动化跨医生 403 已通过 |
| S6-12 | 缺陷复测及遗留问题确认 | 自动缺陷复测已通过，2 个 P1 均关闭；仅人工 UAT 签署未执行 |
| S6-G1 | 阶段 5 定时触发门禁 | 已通过，阶段 5 已关闭 |

## 一键复验

在项目根目录执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\scripts\verify-stage6.ps1
```

该脚本执行阶段 4 全量基线，并校验阶段 6 当前最低测试数、PostgreSQL 集成测试覆盖、导入上限实现和本阶段验收文档。批量性能与人工 UAT 的结果继续追加到本文件。

## 当前风险与遗留项

- 阶段 5 的 11:00 定时门禁已通过并关闭。
- 五类角色人工 UAT 按项目方要求暂不执行；对应的数据范围、查询、修改、导出和越权 403 已有自动化回归证据。
- 性能指标除业务容量外尚未形成医院认可的响应时间阈值；测试先记录基线数据，再由项目方签署最终阈值。

## 第二轮执行记录：导入容量与缺陷整改

执行时间：2026-09-01 10:43～10:46（Asia/Shanghai）

- 欠费真实 Excel 1000 条导入成功，1001 条被明确拒绝且拒绝批次写入患者数为 0；全量回归实测耗时 10.20 秒。
- 预出院真实 Excel 1000 条导入成功，1001 条被明确拒绝且拒绝批次写入患者数为 0；修复后全量回归实测耗时 6.03 秒。
- 发现并关闭缺陷 `S6-DEFECT-001`（P1）：预出院数据未填写预计出院时间时，PostgreSQL 无法推断重复空参数类型，导致导入失败。修复方式为在 SQL 条件中将该参数显式转换为 `TIMESTAMPTZ`。
- 修复后阶段 6 全量复验通过：28 个后端测试，0 失败、0 错误、0 跳过；前端类型检查与生产构建通过，入口 JavaScript 12.06 KB。

## 第三轮执行记录：查询、导出与并发

执行时间：2026-09-01 11:01～11:11（Asia/Shanghai）

- 欠费和预出院 999 条真实 Excel 导入均通过，至此 1/999/1000/1001 条边界全部覆盖。
- 100 个并发请求使用真实 Basic Authentication 访问欠费、预出院核心列表，全部返回 200；全量回归耗时 2.51 秒。
- 1999 条欠费数据：筛选查询 165 毫秒，全量 CSV 导出 163 毫秒。
- 1999 条预出院数据：筛选查询 94 毫秒，全量 CSV 导出 114 毫秒。
- 发现并关闭缺陷 `S6-DEFECT-002`（P1）：欠费导出请求 20000 条时被页面大小白名单回退为 50，实际仅导出前 50 条。现已增加仅供内部导出使用的 20000 条查询入口，同时保持外部列表最大页大小为 200。
- 同步收紧预出院分页：20000 条仅允许导出内部调用，外部列表仍限制为 20/50/100/200。
- 修复后阶段 6 全量复验通过：29 个后端测试，0 失败、0 错误、0 跳过；前端类型检查与生产构建通过，入口 JavaScript 12.06 KB。

## 第四轮执行记录：阶段5关闭与统计性能

执行时间：2026-09-01 11:00～11:24（Asia/Shanghai）

- 阶段 5 的 6 条定时任务均在 11:00 自动生成，并在 11:00:34～11:00:36 首次发送成功；失败尝试为 0，阶段 5 正式关闭。
- 1999 条欠费数据统计报表耗时 107 毫秒。
- 1999 条预出院数据月度分析耗时 115 毫秒。
- 两项统计接口均返回 200，未发现新增缺陷。
- 容量、查询、导出、统计和 100 并发自动化测试至此全部通过。

## 第五轮执行记录：重复导入与推送队列容量

执行时间：2026-09-01 11:26（Asia/Shanghai）

- 连续执行 50 个欠费导入批次全部成功，后 49 次覆盖耗时合计 1.61 秒；缴费状态、欠费原因和追缴进度等人工字段完整保留。
- 创建 1000 条到期推送任务后，调度器按设计安全领取前 100 条，生成 100 条尝试记录，其余 900 条保持 `PENDING`，没有重复领取或任务丢失；首批处理耗时 339 毫秒。
- 推送容量验证使用缺少企微配置的隔离测试环境，前 100 条按预期进入 `RETRYING/CONFIG_MISSING`，未发起真实外部消息。
- 最终执行 `scripts/verify-stage6.ps1`：31 个后端测试全部通过，0 失败、0 错误、0 跳过；Flyway V1～V13、前端类型检查、生产构建和入口包限制全部通过。

## 当前阶段结论

### 主管医生工号展示扩展执行记录（2026-09-02）

- 第一阶段完成后端字段贯通：`ArrearsRecordSummary` 增加 `doctorEmployeeNo`，列表、单条查询和导出查询读取 `patient_encounter.doctor_employee_no`；关键词增加医生工号模糊匹配。数据库字段、索引和导入落库逻辑已存在，无需新增 Flyway 迁移。
- 第二阶段完成页面与导出：欠费明细在“主管医生”后增加“主管医生工号”列，搜索提示增加医生工号，编辑催缴摘要显示主管医生及工号；Excel、CSV 同步增加“主管医生工号”列，空值统一显示“—”。
- 第三阶段完成非集成自动化回归：后端 37 项非集成单元测试通过；前端 6 个测试文件共 17 项通过；TypeScript 类型检查和 Vite 生产构建通过。
- PostgreSQL/Testcontainers 集成回归新增主管医生工号专项用例，验证接口返回、完整/部分关键词匹配链路、工号搜索下的主管医生 DataScope 隔离，以及 CSV 表头和值；`ApplicationPostgresIntegrationTest` 24 项、`PostgresTestcontainersSmokeTest` 1 项均通过，0 失败、0 错误、0 跳过。
- 当前完整后端基线为 62 项：37 项非集成测试、24 项 PostgreSQL 业务集成测试和 1 项 Testcontainers 烟雾测试。
- Docker Desktop 使用 Engine 29 时，Testcontainers 必须解析 `desktop-linux` context 的真实端点，并以 Maven 参数 `-Dapi.version=1.44` 运行；仅设置 `DOCKER_API_VERSION` 环境变量不足。任何集成测试结果必须核对 `Skipped: 0`，不得把全部跳过的 Maven `BUILD SUCCESS` 视为通过。

阶段 5 已完成并关闭。阶段 6 原有范围中的容量、性能、并发、权限自动化、缺陷修复和全量回归均已完成；2026-09-01 新增的欠费明细原型对齐整改第一至第十阶段已完成，最终自动集成回归通过。页面人工 UAT 仍按项目方既定要求不在本轮执行，进入阶段 7 前仅需确认人工验收安排。

## 欠费明细原型对齐第一阶段执行记录

执行时间：2026-09-01 16:07～16:09（Asia/Shanghai）

- 扩展欠费明细返回模型，补充入区/出区日期、总费用、预交金、医保支付、个人账户支付和最近操作人。
- 列表及单条查询补齐数据关联，默认按欠费金额、数据更新时间倒序。
- 编辑欠费记录时写入当前登录用户到 `last_operated_by`，同步更新 `updated_at` 并继续记录统一审计日志。
- PostgreSQL 16/Testcontainers 目标集成测试 18 个全部通过，0 失败、0 错误、0 跳过。
- 完整后端回归共 32 个测试，0 失败、0 错误、0 跳过；第一阶段关闭，进入第二阶段追缴状态结构化。

## 欠费明细原型对齐第二阶段执行记录

执行时间：2026-09-01 16:16～16:23（Asia/Shanghai）

- 新增 Flyway V14，将追缴进度统一为 `NOT_STARTED/NEGOTIATING/REFUSED/LEGAL_ACTION/PAID` 五种编码。
- 迁移时识别既有编码及常用中文状态；迁移前原始文本保留在 `recovery_progress_legacy`，无法识别的数据不静默丢弃。
- 新增最近一次非已缴费状态字段及数据库约束，保证缴费状态与追缴进度一致。
- 标记缴费时保存前一非缴费进度；恢复未缴费时自动恢复该进度，无历史时回到“未催缴”。
- 更新接口拒绝非法追缴进度，请求解析或取值不合法统一返回 400。
- 当前欠费页面编辑控件改为结构化下拉框，缴费状态中文显示，快捷标记/恢复与后端规则兼容。
- PostgreSQL 16/Flyway V1～V14 空库迁移通过；完整后端回归 33 个测试全部通过，0 失败、0 错误、0 跳过；前端类型检查和生产构建通过。

## 欠费明细原型对齐第三阶段执行记录

执行时间：2026-09-01 16:24～16:29（Asia/Shanghai）

- 欠费列表和 CSV 导出接口增加 `departmentId`、`arrearsType`、`feeType`、`recoveryProgress`、`inArrears` 和 `keyword` 组合筛选。
- 关键词查询扩展到住院号、患者姓名和主管医生。
- 新增 `/api/arrears/records/filter-options`，只返回当前 DataScope 内实际存在的科室、费别和欠费类型。
- 页面新增欠费类型、科室、费别、追缴进度和欠费状态筛选；欠费状态默认“当前欠费”，查询、重置、分页和导出复用同一参数集。
- 后端编译通过；前端类型检查和生产构建通过。
- 按项目方要求，本阶段暂不执行 PostgreSQL/Testcontainers 集成回归；相关组合筛选、越权参数和筛选项范围测试留待后续统一执行。

## 欠费明细原型对齐第四阶段执行记录

执行时间：2026-09-01 16:31～16:38（Asia/Shanghai）

- 新增 `/api/arrears/records/summary`，与列表共用关键词、科室、欠费类型、费别、追缴进度、欠费状态和 DataScope。
- 统计返回欠费患者总数、三种欠费类型人数、欠费金额合计、未催缴人数、移交法务人数和最近数据更新时间。
- 页面增加欠费患者数、欠费金额合计、未催缴、移交法务发起诉讼四张卡片。
- 查询、重置、导入成功、编辑保存、标记缴费和恢复未缴费后同步刷新统计与列表；分页变化仅刷新列表。
- 新增 `ArrearsRecordControllerTest`，验证统计接口完整传递筛选参数并使用解析后的 DataScope；1 个相关单元测试通过，0 失败、0 错误、0 跳过。
- 前端类型检查和生产构建通过；按项目方要求未执行 PostgreSQL/Testcontainers 集成回归。

## 欠费明细原型对齐第五阶段执行记录

执行时间：2026-09-01

- 欠费患者列表按原型顺序扩展为 20 列，补齐住院次数、病区、费别、欠费类型、入出区日期、各项费用、应交押金、最近操作人和数据更新时间。
- 病区为空时回退科室；在院患者的医保支付和个人账户支付显示“—”，出院患者展示实际金额。
- 金额统一使用千分位和两位小数并右对齐，欠费金额突出显示；欠费类型和追缴进度转换为中文，追缴进度按状态使用颜色标签。
- 固定住院号、姓名和操作列，表格支持横向滚动；欠费原因等长字段保留悬浮提示，分页规格保持 20/50/100/200。
- 前端类型检查和生产构建通过。前端工程当前未配置单元测试框架，因此本阶段未新增或执行前端单元测试；按项目方要求未执行后端及 PostgreSQL/Testcontainers 集成回归。

## 欠费明细原型对齐第六阶段执行记录

执行时间：2026-09-01 16:52（Asia/Shanghai）

- 编辑弹窗标题调整为“编辑催缴信息”，只读展示姓名、住院号、住院次数、住院病区和当前欠费金额。
- 欠费原因保存前去除首尾空格，保存期间禁用取消并保持提交按钮加载状态；失败时继续保留当前输入。
- “标记缴费/恢复未缴费”增加二次确认，确认信息包含患者姓名和欠费金额。
- 新增 `GET /api/arrears/records/{id}/history`，先按当前用户 DataScope 校验欠费记录访问权限，再按时间倒序返回操作人、时间、类型、修改前后快照和变更说明。
- 编辑弹窗增加操作历史时间线；无记录时显示“暂无操作历史”，打开弹窗及保存成功后刷新历史。
- 定向执行 `ArrearsRecordControllerTest` 和 `ArrearsRecordHistoryServiceTest`，共 4 个单元测试通过，0 失败、0 错误、0 跳过；前端类型检查通过。
- 按项目方要求未执行全量测试、生产构建及 PostgreSQL/Testcontainers 集成回归。

## 欠费明细原型对齐第七阶段执行记录

执行时间：2026-09-01 16:57（Asia/Shanghai）

- 导入按钮文案调整为“导入欠费报表”，前后端统一仅接受 `.xlsx` 文件，不再接受旧版 `.xls`。
- 导入成功后在页面展示批次号、总数、成功、失败、新增、覆盖和跳过数量；页码回到第 1 页，保留当前筛选并刷新统计与列表。
- 后端成功结果增加明确的失败数量字段；成功批次当前失败数为 0。
- 前端请求错误保留后端结构化数据，校验失败时可取得失败批次号和错误数量，并提供“查看导入记录”入口。
- 定向执行 `ArrearsImportControllerTest` 和 `ArrearsImportServiceTest`，共 2 个单元测试通过，0 失败、0 错误、0 跳过；前端类型检查通过。
- 按项目方要求未执行全量测试、生产构建及 PostgreSQL/Testcontainers 集成回归。

## 欠费明细原型对齐第八阶段执行记录

执行时间：2026-09-01 17:07（Asia/Shanghai）

- 页面增加统一“导出”下拉入口，支持“导出 Excel”和“导出 CSV”，两种格式复用当前列表的全部筛选条件。
- 后端导出接口增加 `format=csv|xlsx`，两种格式复用相同的 DataScope、全量筛选查询和 19 列字段转换，不包含操作列。
- 两种格式统一中文状态、两位金额、病区回退、在院患者医保/个人账户支付显示“—”以及其他空值规则。
- CSV 增加 UTF-8 BOM 并继续使用标准 CSV 转义；Excel 使用 EasyExcel 生成真实 `.xlsx` 工作簿。
- 导出文件名包含本地导出时间；筛选结果超过内部 20000 条上限时明确提示缩小筛选范围，不再静默截断。
- 定向执行 `ArrearsExportServiceTest` 和 `ArrearsRecordControllerTest`，共 4 个单元测试通过，0 失败、0 错误、0 跳过；前端类型检查通过。
- 按项目方要求未执行全量测试、生产构建及 PostgreSQL/Testcontainers 集成回归。

## 欠费明细原型对齐第九阶段执行记录

执行时间：2026-09-01 17:11（Asia/Shanghai）

- 补充欠费列表组合筛选、分页参数及 DataScope 解析结果向仓储层完整传递的控制器单元测试，包含主管医生关键词场景。
- 补充编辑前后快照、当前操作人和客户端地址写入审计日志的单元测试，以及操作历史查询前的数据范围校验测试。
- 新增追缴进度和缴费状态编码规范化、非法状态拒绝及 `PAID` 双向联动测试。
- 补充 Excel/CSV 字段转换、中文状态、在院支付空值、CSV BOM/转义、真实 XLSX、中文时间文件名、非法格式及 20000 条安全上限测试。
- 同步复验导入结果及 `.xlsx` 文件限制相关测试。
- 定向执行 6 个相关测试类，共 13 个单元测试通过，0 失败、0 错误、0 跳过。
- 按项目方要求未执行 PostgreSQL/Testcontainers 集成回归、全量回归和页面人工一致性验收；统计/列表/导出真实数据库一致性、角色端到端权限及浏览器交互仍需在后续集成验收中确认。

## 欠费明细原型对齐第十阶段执行记录

执行时间：2026-09-01 17:14～17:18（Asia/Shanghai）

- 将第十阶段定义为前九阶段完成后的最终集成回归与整改关闭，执行既有 `scripts/verify-stage6.ps1` 全量自动验证。
- 首轮回归发现缺陷 `S6-ARREARS-DEFECT-001`：历史导入记录的欠费类型允许为空，新导出映射使用不接受空键的不可变 Map，导致 CSV 导出返回 500。
- 修复导出服务对空欠费类型和空追缴进度的兼容处理，统一导出为“—”，并新增历史空值导出单元测试。
- 修复后 PostgreSQL 16/Testcontainers 集成测试通过，Flyway V1～V14 在空库完整迁移。
- 后端全量测试 47 个通过，0 失败、0 错误、0 跳过；其中 PostgreSQL 业务集成测试 19 个通过，Testcontainers 烟雾测试 1 个通过。
- 前端 `vue-tsc -b` 和 Vite 生产构建通过；入口 JavaScript 12.22 KB，小于 100 KB 门禁。
- 自动化范围内欠费明细原型对齐整改关闭；页面人工 UAT 继续遵循项目方既定安排，本轮不执行。

## 通报报表原型对齐阶段 1～5 执行记录

执行时间：2026-09-01 19:35～22:10（Asia/Shanghai）

- 阶段 1：报表统一使用最新 `SUCCESS + READY` 批次、DataScope 和未缴费口径，新增服务端患者欠费 Top10、范围类型和范围名称。
- 阶段 2：页面增加批次状态、汇总卡、Top3 渐变卡、科室横向排行、服务端 Top10、刷新及加载/错误/空状态。
- 阶段 3：抽取统一通报内容服务，新增只读 `/api/arrears/report/notice-preview`，页面不提供渠道选择、接收人编辑或确认推送。
- 阶段 4：欠费企业微信任务改为每天 08:00，过滤无企微 ID 用户并显式执行同日去重；新增 `/api/arrears/report/export` 三工作表 Excel 导出及审计。
- 阶段 5：完成 Top3 不足、长科室名、大金额、中文欠费类型、窄屏和无障碍属性等边界处理。
- 前端全量 4 个测试文件、8 项测试全部通过；`vue-tsc -b` 和 Vite 生产构建通过。
- 后端全量命令发现 53 项测试，其中 32 项执行通过、21 项 Testcontainers 测试因本机 Docker Engine 未就绪自动跳过，0 失败、0 错误；提升权限单独复验后 Docker 仍返回未就绪状态。
- 本轮不能将 PostgreSQL 集成测试标记为通过；Docker 恢复后需重新执行 20 项业务集成测试和 1 项烟雾测试。
- 完整字段、接口、数据模型边界和测试明细见[通报报表原型对齐实施记录](通报报表原型对齐实施记录.md)。

## 欠费推送记录原型对齐第一阶段执行记录

执行时间：2026-09-02（Asia/Shanghai）

- 删除渠道筛选和渠道列，不将企业微信渠道作为页面查询维度。
- 页面状态固定为发送中、重试中、成功、失败；其中 `PENDING/SENDING` 合并为发送中，`RETRYING`、`SENT`、`FAILED` 分别映射为重试中、成功、失败。
- `CANCELLED` 保留为底层兼容状态，不进入筛选项，不错误映射为失败。
- 推送日期查询和列表排序统一使用 `COALESCE(sent_at, scheduled_at)`；日期范围采用左闭右开边界。
- 科主任只能查看配置科室相关推送，主管医生只能查看本人相关推送；系统管理员、运营部和财务科可查看全院。
- 查看权限与重发权限拆分；科主任和主管医生只读，系统管理员、运营部和财务科可重发。
- 批量重发限定当前页勾选的失败任务，不再使用重发全部失败任务的语义。
- 已形成后续数据范围模型、查询接口、批量重发接口和阶段 2～7 实施顺序。
- 本阶段仅固化业务口径和接口约定，未修改业务代码；按要求未执行集成测试、回归测试及其他自动化验证。

## 欠费推送记录原型对齐第二阶段执行记录

执行时间：2026-09-02（Asia/Shanghai）

- 新增 Flyway V15，在推送任务中固化接收用户和 `ALL/DEPARTMENT/DOCTOR/MIXED/LEGACY_UNRESOLVED` 范围类型。
- 新增 `push_task_scope`，支持一条任务关联多个科室以及主管医生本人范围，并补充约束、去重索引和查询索引。
- 历史任务只按企微 ID 安全回填接收用户，不猜测正文的数据范围，统一标记为 `LEGACY_UNRESOLVED`。
- `DataScopeService` 增加后台任务可复用的按用户 ID 范围解析；登录请求继续使用同一实现。
- 欠费定时任务改为按接收用户逐人解析 DataScope、生成正文并固化范围，不再向科主任和主管医生发送同一份全院正文。
- 科主任支持多科室并集，主管医生按本人用户 ID，多角色用户继续按既有并集规则处理；无有效范围或范围内无欠费数据时不创建任务。
- 任务及范围关联在同一事务内写入，同日同接收人防重规则保持不变。
- 阶段 3 的列表/详情 DataScope 和查看权限拆分尚未实施；按要求未执行集成测试、回归测试、单元测试、类型检查、编译或生产构建。

## 欠费推送记录原型对齐第三阶段执行记录

执行时间：2026-09-02（Asia/Shanghai）

- 新增 Flyway V16 及独立 `API_PUSH_RECORD_VIEW`，运营部、财务科、科主任、主管医生获得查看权限，系统管理员继续通过系统角色访问。
- 欠费和预出院推送菜单、路由改用查看权限；重发操作仍使用独立重发权限。
- 列表和发送尝试详情接入 DataScope；科主任按固化科室范围、主管医生按固化医生用户 ID 查看，多角色取并集。
- `LEGACY_UNRESOLVED` 历史任务只允许全院角色查看；尝试详情在返回数据前再次校验任务范围。
- 列表增加上海时区推送日期范围，统一按 `COALESCE(sent_at,scheduled_at)` 查询和倒序排列。
- 实现发送中、重试中、成功、失败四种业务状态映射，并兼容当前页面过渡期使用的 `PENDING/SENT` 参数。
- 列表响应补充实际正文摘要、中文状态、最近触发方式和统一推送时间。
- 前端 `npm run typecheck` 通过；当前环境未提供 Maven/Java 可执行文件，后端跳过测试编译无法执行。
- 阶段 3 完成时重发接口安全整改尚未实施；该能力现已在阶段 4 完成。阶段 3 按要求未执行单元测试、集成测试、回归测试或生产构建。

## 欠费推送记录原型对齐第四阶段执行记录

执行时间：2026-09-02（Asia/Shanghai）

- 新增 Flyway V17，将 `API_PUSH_RETRY` 的资源范围修正为推送记录下全部 POST 重发端点。
- 安全链和控制器统一使用重发权限；单条和批量操作均解析当前 DataScope，并校验任务可见性。
- 单条重发增加业务类型、失败状态和条件更新校验，并发状态变化时不重复重发。
- 批量接口取消“重发业务下全部失败任务”的旧语义，改为接收 `businessType` 和 1～200 个明确任务 ID。
- 逐条校验任务存在/可见性、页面业务类型和失败状态；重复 ID、非失败状态和并发状态变化计入跳过。
- 批量返回请求、成功、跳过和失败数量；成功任务逐条记录更新前后快照、操作人、IP 和任务 ID。
- 批量更新和审计处于同一事务；未处理异常时整体回滚，既有发送尝试保留并由后续调度追加人工尝试。
- 阶段 4 完成时前端勾选与确认交互尚未实施；上述能力现已在阶段 5 完成。
- 当前环境未提供 Maven/Java 可执行文件，后端跳过测试编译无法执行；按要求未执行单元测试、集成测试、回归测试或生产构建。

## 欠费推送记录原型对齐第五阶段执行记录

执行时间：2026-09-02（Asia/Shanghai）

- 页面调整为查询卡片和推送留痕列表卡片，删除渠道筛选与渠道列。
- 筛选区增加四种中文业务状态、推送日期范围、查询和重置；主表使用统一推送时间、内容摘要、中文状态和触发方式。
- 主表保留重试次数、最近错误、分页及尝试详情；详情内状态和自动/人工触发方式同步中文化。
- 有重发权限时仅允许勾选当前页失败记录；无重发权限时隐藏选择、单条重发和批量重发，页面保持只读。
- 单条和批量操作均增加二次确认；批量按 `{businessType, ids}` 提交并展示成功、跳过、失败数量。
- 查询、重置、分页及重新加载后清空选择，避免对旧页面状态中的任务误操作。
- 新增推送页面定向单元测试，覆盖筛选请求、批量请求/结果及只读权限，共 1 个测试文件、3 项测试通过。
- 前端 `npm run typecheck` 通过；按要求未执行其他单元测试、后端测试、集成测试、回归测试或生产构建。

## 欠费推送记录原型对齐第六阶段实施记录

执行时间：2026-09-02（Asia/Shanghai）

- 抽取 `PushRecordPolicy` 供生产控制器与测试共用，集中管理状态归并、中文展示、内容摘要和业务类型白名单。
- 新增后端策略测试 3 项及控制器参数校验测试 2 项，并修订定时任务相关测试 1 项，共 3 个相关测试类、6 项单元测试；均不依赖数据库或 Testcontainers。
- 前端推送页面测试扩展为 6 项，增加失败行选择限制、重置查询条件、取消确认不请求和尝试详情中文化覆盖。
- 定向执行前端 1 个测试文件、6 项测试全部通过；`npm run typecheck` 通过。
- 当前环境未提供 Maven/Java，后端 3 个定向测试类已编写但无法执行，待 Java 环境恢复后按类名定向复验。
- 按要求未执行其他前端测试、后端全量测试、PostgreSQL/Testcontainers 集成测试、回归测试或生产构建；阶段 7 仍待实施。

## 欠费推送记录原型对齐第七阶段完成记录

执行时间：2026-09-02（Asia/Shanghai）

- 新增科主任推送任务 DataScope、尝试详情越权、只读权限及批量重发边界 PostgreSQL 集成测试。
- 新增预出院入口复用推送页面的前端业务类型测试。
- PostgreSQL 16/Flyway V1～V17 空库迁移及 23 项业务集成测试通过，0 跳过。
- 后端全量 61 项测试通过；前端全量 5 个测试文件、15 项测试、类型检查和生产构建通过。
- `scripts/verify-stage6.ps1` 全量门禁通过，欠费推送记录原型对齐阶段 1～7 自动化整改关闭。

## 预计出院管理原型对齐第六阶段执行记录

执行时间：2026-09-03（Asia/Shanghai）

- 本阶段执行定向功能验证与收口，覆盖预计出院列表、组合筛选、同源统计、详情与操作历史、编辑校验、异常规则、会诊和随访页面。
- 后端定向执行 `DischargeControllerTest`、`DischargeRepositoryTest`、`DischargeTimeTypeTest`、`DischargeUpdateValidatorTest` 和 `DischargeAbnormalCalculatorTest`，共 18 项测试通过，0 失败、0 错误、0 跳过。
- 前端定向执行 `DischargeManagementView.test.ts` 和 `DischargeCareViews.test.ts`，共 2 个测试文件、5 项测试通过。
- 本阶段未发现需要整改的功能缺陷，未新增数据库迁移或业务代码修改。
- 按要求未执行 PostgreSQL/Testcontainers 集成测试、后端或前端全量回归、类型检查及生产构建；本记录不得作为完整集成或回归通过证据。

## 预计出院管理原型对齐第七阶段执行记录

执行时间：2026-09-03（Asia/Shanghai）

- 预计出院 CSV 导出改为复用列表的关键词、科室、时间类型、日期区间、出院状态和 DataScope，不再只传关键词与出院状态。
- 导出字段扩展为与页面列表一致的 16 个业务字段，并增加 UTF-8 BOM，避免中文表头在常用表格软件中乱码。
- 预计出院导入前后端统一只接受 `.xlsx` 文件，移除旧版 `.xls` 入口，并在进入 Excel 解析或数据库访问前拒绝错误格式。
- 新增 `DischargeExportControllerTest` 和 `DischargeImportServiceTest`，定向执行 2 项后端单元测试，全部通过，0 失败、0 错误、0 跳过。
- 定向执行 `DischargeManagementView.test.ts`，1 个前端测试文件、3 项测试全部通过，其中包含 `.xlsx` 上传限制校验。
- 按要求未执行 PostgreSQL/Testcontainers 集成测试、其他后端或前端测试、全量回归、类型检查及生产构建；本记录不得作为完整集成或回归通过证据。

## 预计出院管理原型对齐第八阶段完成记录

执行时间：2026-09-03（Asia/Shanghai）

- 第八阶段按前七阶段完成后的最终集成回归与整改关闭执行，运行 `scripts/verify-stage6.ps1` 完整自动化门禁。
- Docker Desktop Engine 29.2.1 与 Testcontainers 1.21.4 API 协商正常；PostgreSQL 16 容器成功启动，Flyway V1～V19 在空库完整迁移。
- 后端全量 84 项测试全部通过，0 失败、0 错误、0 跳过；其中 `ApplicationPostgresIntegrationTest` 25 项、`PostgresTestcontainersSmokeTest` 1 项。
- 前端全量 11 个测试文件、30 项单元测试全部通过；`vue-tsc -b` 类型检查和 Vite 生产构建通过。
- 第七阶段新增的导出完整筛选、16 字段 CSV、UTF-8 BOM 以及 `.xlsx` 导入限制通过全量验证，未发现需要继续整改的缺陷。
- 预计出院管理原型对齐第一至第八阶段自动化范围关闭；后续人工 UAT 和生产环境验证不包含在本次自动化结论中。

## 预计出院管理患者工作台补充整改记录

执行时间：2026-09-03（Asia/Shanghai）

### 第一阶段：导航与旧链接兼容

- 从“预出院管理”左侧菜单移除“会诊预约管理”和“出院随访填报”。
- 保留“预计出院管理、统计分析、导入记录、推送记录”。
- 旧会诊和随访地址重定向到预计出院管理，并保留 `encounterId`、`recordId` 及目标业务区块参数。

### 第二、三阶段：统一患者工作台与组件化

- 预计出院患者填报/编辑弹窗调整为主管医生、门诊部、营养科、居家康复科、随访员和操作历史六个区块，不再二次搜索患者或跳转独立页面。
- 营养会诊和居家康复支持患者上下文内多记录查询、新增、修改和逻辑删除，最多 10 条。
- 抽取 `ConsultationSection` 和 `FollowUpSection` 复用组件，删除已无路由引用的旧独立会诊、随访页面。
- 会诊维护完成后联动刷新预计出院列表、顶部统计和患者操作历史。

### 第四阶段：字段级权限

- 患者详情对具备预出院查看权限的角色开放；操作入口不再由单一编辑权限整体隐藏。
- 主管医生、门诊部、营养科、居家康复科和随访员分别使用 `FIELD_ATTENDING_DOCTOR`、`FIELD_OUTPATIENT`、`FIELD_NUTRITION`、`FIELD_HOME_REHAB`、`FIELD_FOLLOW_UP` 控制编辑能力。
- 前端只提交当前角色有权编辑的字段；后端控制器再次执行字段权限校验，安全链允许对应字段权限访问相关更新接口。
- 新增 Flyway V20，登记门诊复诊字段权限；无字段权限修改返回 403，授权后允许修改。

### 第五阶段：门诊与随访结构化字段

- 门诊复诊补齐是否到诊、具体到诊时间、填报人和未到诊原因；已到诊必须填写到诊时间，未到诊必须填写原因。
- 第 7、30、60 天随访均包含回访时间、执行人、恢复情况、复诊提醒、居家康复评价与评估预约、营养评价与评估预约；评价“不满意”时原因必填。
- 保留原随访文本字段用于兼容已有数据，新增 JSONB 结构化随访明细。
- 新增 Flyway V21，增加门诊跟踪字段和结构化随访字段。

### 第六阶段：集成与回归验收

- 前端全量 12 个测试文件、33 项单元测试全部通过；`vue-tsc -b` 类型检查和 Vite 生产构建通过。
- 后端全量 85 项测试全部通过，0 失败、0 错误、0 跳过。
- `ApplicationPostgresIntegrationTest` 25 项和 `PostgresTestcontainersSmokeTest` 1 项全部实际执行，合计 26 项容器集成测试，0 跳过。
- Docker Desktop Engine 29.2.1、Testcontainers 1.21.4 和 PostgreSQL 16.13 验证通过；Flyway V1～V21 在空库完整迁移。
- 集成测试同步修订旧权限预期：可见患者但缺少字段权限时修改返回 403，授予相应字段权限后修改成功。

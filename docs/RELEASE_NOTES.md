## V1.1.8 — 2026-09-08

对比基准：V1.1.7。

### 新增功能

- 角色权限配置支持按菜单、接口/字段及科室筛选，并支持分组全选或取消全选。
- 预计出院列表返回并展示主管医生工号，方便识别医生主数据。

### 问题修复

- 出院看板仅展示已填报预计出院日期且尚未出院的患者，并按预计出院时间升序排列。
- 导入预计出院数据后同步重算异常状态，避免导入记录的异常标识滞后。
- 预计出院预约时间的修改权限归属主管医生字段；实际到诊相关字段仍归属门诊部字段权限。

### 配置与数据库

- 不新增数据库迁移，不修改运行配置或业务数据。

### 已知问题

- 无。
## V1.1.7 — 2026-09-07

对比基准：V1.1.6。

### 新增功能

- 构建时自动注入前端和后端版本号，页面顶栏与系统信息接口使用构建产物版本。

### 问题修复

- 消除页面和系统信息接口的硬编码版本号，避免发布后仍显示旧版本。

### 配置与数据库

- 不新增数据库迁移，不修改测试环境运行配置或业务数据。

### 已知问题

- 无。

## V1.1.6 — 2026-09-07

对比基准：V1.1.5。

### 新增功能

- 无。

### 问题修复

- 修正页面顶栏和系统信息接口仍显示 V1.1.2 的问题，统一显示实际发布版本 V1.1.6。

### 配置与数据库

- 不新增数据库迁移，不修改测试环境运行配置或业务数据。

### 已知问题

- 版本展示仍需在每次发布时同步更新；后续可改造为由构建版本自动注入。

## V1.1.5 — 2026-09-07

对比基准：V1.1.3。

### 新增功能

- 用户管理新增编辑入口，可修改姓名、工号和所属科室；登录名与企微 ID 在编辑时保持只读。
- 科室管理新增编辑入口，可修改科室名称；科室编码在编辑时保持只读。

### 问题修复

- 后端新增用户与科室更新接口，校验用户工号唯一性及所属科室启用状态。
- 科室更新请求与 SQL 均不包含科室编码，防止通过接口篡改稳定业务标识。

### 配置与数据库

- 不新增数据库迁移，不修改测试环境运行配置或业务数据。

### 已知问题

- 科室编码如需变更，仍需通过受控的数据迁移流程处理，管理页面不提供直接修改入口。

# 发布说明
## V1.1.3 — 2026-09-06

对比基准：V1.1.2。

### 新增功能

- 无。

### 问题修复

- 欠费明细和预计出院管理的导入失败提示现在明确显示首项校验错误的行号、字段、原值及原因；多项错误可提示剩余数量并跳转到对应导入批次查看全部明细。

### 配置与数据库

- 不新增数据库迁移，不修改测试环境运行配置或业务数据。

### 已知问题

- 批次详情仍保留为逐项错误查看入口；用户需修正源文件或主数据后重新导入。


## V1.1.2 — 2026-09-06

对比基准：V1.1.1。

### 新增功能

- 系统管理的科室管理支持按科室编码、科室名称和状态组合检索，并提供服务端分页（20、50、100、200 条）。

### 问题修复

- 欠费明细导入遇到同名且启用的科室时不再因查询结果多条而整体回滚；系统按最早创建的匹配科室稳定归属记录。

### 配置与数据库

- 不新增数据库迁移，不修改测试环境运行配置；发布保留既有业务数据。

### 已知问题

- 历史同名科室数据仍需后续治理；在未完成合并前，欠费导入会选用最早创建的启用科室。

## V1.1.1 — 2026-09-05

对比基准：V1.1.0。

### 新增功能

- 用户管理和科室管理支持单个删除、批量删除、批量启用及批量停用。
- 顶部导航调整为帮助入口、版本标识和用户下拉菜单；科室管理恢复模板导出、数据导出及导入入口。

### 问题修复

- 用户登录名改为企微 ID；默认密码和重置密码统一为 `kfyy123!`。
- 用户与科室导入成功后显示文件名、总数、成功数和失败数；导入后的列表刷新失败不再误报为导入失败。
- 欠费导入兼容多工作表、源表别名和公式金额；预出院导入兼容源表字段、医生工号、超过 1,000 条的数据及重复科室名称。

### 配置与数据库

- 不新增数据库迁移。测试环境部署前保留现有数据和环境配置。

### 已知问题

- 预出院源表的医生工号需与系统用户工号一致；不一致的记录可以导入，但医生关联状态为未匹配。

## V1.1.0 — 2026-09-05

对比基准：v1.0.0。新增移动端统一工作台入口、浏览器密码管理器记住密码、用户与科室 Excel 模板导出/数据导出/数据导入、右上角版本及用户手册入口、修改密码快捷入口。

### 新增功能

- 手机端和 Web 端登录成功后均默认进入工作台。
- 记住密码仅使用浏览器或设备的凭据管理器保存，不在系统本地存储明文密码。
- 用户管理支持模板导出、用户导出和 xlsx 导入；科室管理支持同类操作。
- 右上角显示 V1.1.0，并提供用户手册下载和修改密码入口。
- 新增覆盖全系统的版本化 Word 用户手册，包含登录、业务模块、系统管理、移动端、常见问题和修改记录。

### 配置与数据库

- 不涉及数据库结构或运行配置变更。
- 测试环境部署记录在部署完成后追加至 docs/DEPLOYMENT_HISTORY.md。

### 已知问题

- 用户手册已生成；当前本机缺少捆绑 LibreOffice 渲染器，无法完成自动页面渲染复核，发布前需在具备渲染器的构建机复核版式。

## v1.0.0 — 2026-09-05

首个正式编号版本，建立当前测试环境的发布基线。对比基准：此前未编号测试版本；Git 基准为 `248336b1ce8e29c51d1ed68d6b0177f090d48a8d`。版本标签按本次用户指定采用小写 `v1.0.0`，前后端包版本为 `1.0.0`。

### 初始功能范围

- 欠费管理：欠费明细、推送记录、通报报表及相关权限控制。
- 预出院管理：患者工作台、会诊、随访、出院提醒和分析。
- 系统管理：用户、角色、字段权限、审计及费用系数配置。
- 企业微信消息集成与计划任务；实际凭据通过环境变量配置。
- H5 响应式导航和核心业务页面适配，移动端表单与会话草稿保护。

### 问题修复与安全改进

- Session 登录与 CSRF Token 校验，可信来源校验及失效会话处理；权限、账号状态和密码变更及时影响已有会话。
- 欠费和预出院更新增加并发冲突提示，避免无提示覆盖。
- 权限管理交互与前后端验证完善，页面安全响应头和相关回归测试补齐。
- 修复测试环境 HTTPS 由 Nginx 转 HTTP 后，缺少可信来源配置导致登录报“请求来源不受信任”的部署问题。

### 配置与数据库

- 腾讯云环境必须设置 `APP_ALLOWED_ORIGINS=https://kangfu.pkucarewiki.cn`；修改后重启 `kangfu-test`。
- HTTPS 环境应配置 `SESSION_COOKIE_SECURE=true`；离线 HTTP 环境按实际协议配置。不得关闭来源校验或 CSRF 防护。
- 数据库基线为 Flyway V1～V23；本次版本流程补齐不新增迁移。升级已有测试环境不重建或清空数据。
- 环境文件、密钥、患者数据及构建缓存不提交 Git。

### 发布与验证

- 发布前回归：后端 107 项通过（0 失败、0 错误、0 跳过）；前端 18 个文件、59 项通过，类型检查和生产构建通过。前后端并行执行时两项前端测试超时，单工作进程重跑全部通过，未放宽超时或修改断言。

- 先提交并推送代码和本说明，再创建并推送 `v1.0.0` 标签；从标签导出的干净源码执行测试和构建。
- 构建产物、提交编号及 SHA-256 清单一同归档，部署到独立目录，保留上一发布以便回滚。
- 具体测试结果、部署时间和提交编号见 `docs/DEPLOYMENT_HISTORY.md`；测试环境配置见 `docs/TENCENT_CLOUD_TEST_DEPLOYMENT.md`。
- 此前 2026-09-05 14:15 的部署未完成提交/标签流程；本次重新从标签构建部署，不追认旧产物来自后来创建的标签。

### 已知问题与验收边界

- 真机 iOS、Android、企业微信、院内网络、角色 UAT、灰度及回滚演练仍须按 `docs/h5/H5-UAT与上线检查清单.md` 完成。
- 自动化登录来源验证使用空账号，不等同于真实账号完整登录验收。
- 该版本供测试与演示，尚未完成生产上线验收。
## V1.2.0 — 2026-09-09

对比基准：V1.1.8。

### 新增功能

- 新增 HIS 接口同步能力，支持在院患者欠费、出院患者欠费和患者信息的手动及每日自动同步。
- 新增 HIS 同步配置页面、接口同步权限、运行互斥、审计记录及导入批次追踪。
- Web 与 H5 页面同步增加 HIS 手动同步入口、医保类型展示、同步配置卡片和接口批次详情。
- 预计出院信息导入同时支持 `.xls` 和 `.xlsx` 文件，并独立导入性别、主诊断及医保类型。

### 问题修复

- 预出院看板展示所有已填报预计出院时间的记录，不再因已经实际出院而遗漏。
- 预计出院列表补齐性别、主诊断和医保类型的查询、接口映射与 Web/H5 展示。
- 修正预计出院 Excel 中“医保类型”被当作欠费模块“费别”处理的问题。

### 配置与数据库

- 新增 Flyway V24：患者住院信息增加医保类型，导入批次增加接口交易码和触发方式，新增 HIS 同步配置表及相关权限。
- 新增 `HIS_GATEWAY_URL`、`HIS_ORGANIZATION_CODE`、`HIS_APPLICATION_ID`、`HIS_LICENSE_ID`、超时和分页环境参数；密钥与机构参数不写入仓库。
- 三类自动同步默认关闭；数据库升级保留既有业务数据，不重建、不清空患者数据。

### 已知问题

- 测试环境在配置真实 HIS 网关、机构号和许可信息前，手动接口同步会返回配置缺失提示，自动同步保持关闭。
- 既有患者记录中为空的性别、主诊断和医保类型不会凭空补齐，需要重新导入或执行患者信息同步。
